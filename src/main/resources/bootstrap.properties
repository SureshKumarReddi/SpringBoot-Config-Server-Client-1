server.port = 2222


spring.cloud.config.uri=http://localhost:9090

spring.profiles.active=dev

#spring.config.import=configserver:

#spring.config.import=optional:configserver:http://localhost:9090


spring.application.name= cloudconfig

# The application specific properties will be read from the git repository, 
# so this file is no longer required. You will to add a new file, bootstrap.properties, 
# in place of application.properties inside the resource folder. 
# This file will be loaded during the service start process and will be used to access the cloud config server. 